let y = [];
console.log(Array.isArray(y));

let z = {};
console.log(typeof z);

const numeros = [10, 20, 30];
console.log(numeros[0]);

const produto = {
  nome: "Caneta",
  preco: 2.5,
};

console.log(produto.nome); //forma mais comum de acessar
console.log(produto["preco"]);
